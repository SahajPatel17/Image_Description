# Uncomment the next line to define a global platform for your project
platform :ios, '17.0'

target 'Imagify' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Imagify
  pod 'Alamofire', '~> 4.0'
  pod 'SwiftyJSON', '~> 4.0'
  target 'ImagifyTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ImagifyUITests' do
    # Pods for testing
  end

end
